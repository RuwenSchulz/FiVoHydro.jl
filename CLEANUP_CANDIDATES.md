# FiVoHydro.jl — cleanup (APPLIED 2026-08-22)

**Status: the deletions below were applied on 2026-08-22** (see "what was done" per row). The
inventory text is kept so the reasoning and the exact commands remain on record. Sizes/dates are from
the submodule at `bb605c2`. `git rm --cached` removes a file from the index but leaves it on disk; the
`.gitignore` lines were added so it does not come back. History is untouched (a rewrite is a separate
decision, and nothing here was deleted from disk).

## A. Generated artifacts that are tracked (≈20 MB, 126 files)

| path | what | size | last commit | referenced by |
|---|---|---|---|---|
| ✅ **UNTRACKED** `snapshots/` | solver CSV/JLD2 dumps from runs in Mar–Apr 2026 (122 files, 12 `.jld2` ≈15 MB) | 20 MB | Apr 2026 | `scripts/plot_is_vs_bdnk.jl` defaults (to files that are NOT among the tracked ones), `tools/snapshot_splines.jl`, `tools/analyze_snapshot_causality.jl` defaults (dirs that do not exist) — i.e. nothing that works today |
| ✅ **UNTRACKED** `debug_flux/` (2 CSV) | flux-decomposition dump from a 2026-04-12 run | 12 KB | Apr 2026 | nothing |
| ✅ **UNTRACKED** `debug_primfail/` (2 CSV) | primitive-recovery failure window, same run | 12 KB | Apr 2026 | nothing |
| ⛔ **KEPT** `last_ic_diagnostics.env` | "Autogenerated by run_ic_diagnostics.sh on 2026-04-10"; the operating point `main()` falls back to when an ENV var is unset (NUR_CLIP_FACTOR=0.98, ALPHA_FILTER_EPS=0.01, …) | 1.5 KB | 2026-04-10 | `main.jl main()` via `diag_*` readers — **behavioural**: deleting it changes `main()` defaults for CLI runs (not for `run_sim_ideal_diff_visc` callers, which pass kwargs). Keep tracked or move the values into documented code defaults first. |

```sh
# APPLIED 2026-08-22, inside Julia/FiVoHydro.jl:
git rm -r --cached snapshots debug_flux debug_primfail     # 126 files, ~20 MB — still on disk
printf 'snapshots/\ndebug_flux/\ndebug_primfail/\n*.jld2\n*.jls\n' >> .gitignore
```
`last_ic_diagnostics.env` was NOT removed: `main()` reads it through `DIAG_ENV_FILE`
(`main.jl:797-798`) as the fallback for every unset ENV knob, so deleting it silently changes CLI
defaults. Retire it together with `run_ic_diagnostics.sh`, or move its values into code defaults first.

## B. Orphaned / misplaced source

| path | what | size | last commit | referenced by | recommendation |
|---|---|---|---|---|---|
| ⛔ **KEPT** `mainBGonly.jl` | background-only variant of `main.jl`; duplicates 10 of its functions (~900 lines); **now declares `module hydro_bgonly`** (was `module hydro`, shadowing `main.jl` if both were included) | 39 KB | 2026-04-10 | 🔴 **`Projects/MainFiVo/Code/case_specs.jl:81`** — the `:background_only` pipeline mode (the 08-21 survey's "referenced by nothing" was WRONG) | keep while MainFiVo's pipeline is alive; retire together with that project |
| ✅ **DELETED** `test_bdnk.jl` (root) | 15-line script that runs a 200-cell BDNK sim to τ=3 into `snapshots/BDNK_test`; not a test, not run by `test/runtests.jl` | 0.4 KB | 2026-03-26 | nothing | delete, or move to `scripts/run_bdnk_example.jl` |
| `run_ic_diagnostics.sh` | 1369-line interactive bash wrapper around `bench/ic_diagnostics.jl` + `main()`; writes `last_ic_diagnostics.env` | 49 KB | 2026-03-11 | `README.md` (old), `init_run.sh` | keep only if the interactive IC workflow is still used; otherwise retire together with `last_ic_diagnostics.env` (see A) |
| ✅ **DELETED** `tools/analyze_fivo_wavefront_speed.py`, `tools/analyze_snapshot_front_speed.py` | Python front-speed analysers; no `requirements.txt`, superseded by `bench_bdnk_causal_signal.jl` / `bench_bdnk_production.jl` in FiVoBenchmark | 14 KB | 2026-01-20 | nothing | delete or add a one-line dependency note |
| `scripts/plot_is_vs_bdnk.jl` | IS-vs-BDNK overlay; defaults pointed at files that no longer exist (now requires explicit `--is`/`--bdnk`) | 7 KB | 2026-03-26 | nothing | keep (works with explicit args) |
| `test/runtests.jl` 2nd-moment block | **removed 2026-08-21** (permanently-skipped `main3.jl` testset) | — | — | — | done |
| `src/dissipation.jl::compute_dvdr!`, `src/ic_diagnostics.jl::_finite_or_nan` | **removed 2026-08-21** (unreferenced) | — | — | — | done |

## C. Duplication map (future refactor, no action now)

- `main.jl` ↔ `mainBGonly.jl`: `charge_integral_Dtau`, `compute_dt_from_work`, `_conservative_remap_U!`,
  `_expand_grid_uniform!`, `_find_last_good_cell`, `initialize!`, `main`, `_resize_grid_uniform!`,
  `_tail_expand_trigger`, `_velocity_stats` — NOT resolvable by deletion (MainFiVo uses it, see B);
  the real fix is to make `:background_only` a `run_sim_ideal_diff_visc` flag and drop the copy.
- `main2M1.jl` ↔ `main2M2.jl`: `_build_h_table!`, `_clamp_eval`, `compute_dUdt!`, `ebar`, `effective_DsT`,
  `_faces`, `_gauss_legendre`, `h_fast`, `_kx`, `_snapshot` — a shared `src/maxent_common.jl` would
  remove ~10 functions; both modules are deliberately parallel to IS2 (CMExperiment pattern), so only
  do this once M2 stabilises.
- The eight `main*.jl` modules each re-implement grid / snapshot / background-loading scaffolding
  instead of sharing `src/` (IS2 includes only utils/grid/constants/eos/io/is2_second_moment_builder).
- `Projects/FiVoBenchmark/bench_common.jl::build_model` mirrors `main.jl:582-600` positionally; a
  keyword constructor on `IdealDiffViscModel` in `src/primitives.jl` would let both call one builder
  (and would also retire the `Vararg{Any,36}` compatibility constructor).

## D. Project-level (outside the submodule) — see `Julia/Projects/MainFiVo/CLEANUP_CANDIDATES.md`

✅ APPLIED 2026-08-22: `Projects/MainFiVo/output` untracked (1114 files, 113 MB `.jld2` + 51 MB CSV,
still on disk, `output/` gitignored); `Projects/FiVoFluidumComparison/data/`'s 8 absolute symlinks
converted to relative (all 11 verified resolving). `Projects/trash/` (42 retired FiVo consumers,
1.4 MB) left alone — it is the user's archaeology convention.
